spring:
  mail:
    host: smtp.qq.com
    username: 2629016097@qq.com
    password: "knwwcqfyueypdiag" # 请确认使用的是授权码
    port: 465
    default-encoding: UTF-8
    protocol: smtps
    properties:
      mail.smtp.connectiontimeout: 5000
      mail.smtp.timeout: 3000
      mail.smtp.writetimeout: 5000
      mail.smtp.auth: true
      mail.smtp.ssl.enable: true  # 添加这一行以启用SSL

